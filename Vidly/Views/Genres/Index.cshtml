@model IEnumerable<Vidly.Models.Genre>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
@*<table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id })
            </td>
        </tr>
    }

    </table>*@

<table id="tbGenres">
    <thead></thead>
    <tbody></tbody>
</table>

@section scripts
{
    <script>
        $(document).ready(function () {
            var dt = $("#tbGenres").DataTable({
                ajax: {
                    url: "/api/genres",
                    dataSrc: ""
                },
                columns: [
                    { data: "name" },
                    {
                        data: "id",
                        render: function (data) {
                            return "<a href='/genres/edit/" + data + "'>Rename</a>";
                        }
                    },
                    {
                        data: "id",
                        render: function (data) {
                            return "<button class='btn btn-danger js-del' data-genre-id='" + data + "'>Excluir</button>";
                        }
                    }

                ]
            });


            $("#tbGenres").on("click", ".js-del", function () {
                var btn = $(this);

                $.ajax({
                    ur: "/api/genres/" + btn.attr("data-genere-id"),
                    type: "DELETE",
                    success: function (data) {
                        bootbox.alert("Excluído!");
                        dt.row(btn.parents("tr")).remove();
                        dt.draw();
                    }
                });
            })
        });
    </script>
}
